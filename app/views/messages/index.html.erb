
  <% @messages.each do |message| %>
  <div class="container">
    <h2><%= message.title %></h2>
    <p><%= message.description %></p>
    <%= link_to "View Message", message_path(message) %>
  </div>
  <% end %>
